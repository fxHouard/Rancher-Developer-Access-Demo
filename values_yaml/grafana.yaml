# Grafana (AppCo variant)
adminPassword: demo

global:
  imagePullSecrets:
  - application-collection

service:
  type: NodePort

sidecar:
  dashboards:
    enabled: true
  datasources:
    enabled: true
