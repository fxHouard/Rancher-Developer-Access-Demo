# values-dev.yaml — Configuration dev locale
auth:
  database: demo
  postgresPassword: demo
  postgresUsername: demo
  username: demo
  password: demo
global:
  imagePullSecrets:
  - application-collection
