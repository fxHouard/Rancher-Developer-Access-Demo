# Grafana (AppCo variant)
adminPassword: demo
image:
  tag: "12.4.2-3.8"

global:
  imagePullSecrets:
  - application-collection

service:
  type: NodePort

sidecar:
  dashboards:
    enabled: true
  datasources:
    enabled: true
