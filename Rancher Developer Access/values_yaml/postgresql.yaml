# PostgreSQL for message-wall (AppCo variant)
auth:
  database: demo
  postgresPassword: demo
  postgresUsername: demo
  username: demo
  password: demo
image:
  postgresql:
    tag: "18.3-8.1"

persistence:
  resources:
    requests:
      storage: 1Gi

global:
  imagePullSecrets:
  - application-collection
